<!DOCTYPE html>
<html lang="nl">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Pokémon Login</title>
  <style>
    body {
      margin: 0;
      padding: 0;
      font-family: 'Verdana', sans-serif;
      background: url('https://assets.pokemon.com/assets/cms2/img/misc/virtual-backgrounds/masters/explore-pikachu.jpg') no-repeat center center fixed;
      background-size: cover;
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
    }

    .login-container {
      background-color: rgba(255, 255, 255, 0.9);
      padding: 30px;
      border-radius: 20px;
      box-shadow: 0 0 20px #fdd835;
      text-align: center;
      max-width: 350px;
      width: 100%;
    }

    .login-container h1 {
      margin-bottom: 20px;
      color: #e53935;
    }

    .login-container input[type="text"],
    .login-container input[type="password"] {
      width: 100%;
      padding: 12px;
      margin: 10px 0;
      border: 2px solid #fbc02d;
      border-radius: 10px;
      font-size: 16px;
    }

    .login-btn {
      background: radial-gradient(circle at center, #f44336 50%, #000 51%);
      color: white;
      border: none;
      padding: 12px 24px;
      font-size: 18px;
      border-radius: 50%;
      cursor: pointer;
      margin-top: 15px;
    }

    .login-btn:hover {
      transform: scale(1.1);
      transition: 0.2s;
    }

    .error {
      color: red;
      margin-top: 10px;
    }
  </style>
</head>
<body>
  <div class="login-container">
    <h1>Pokémon Login</h1>
    <input type="text" id="username" placeholder="Trainernaam" />
    <input type="password" id="password" placeholder="Wachtwoord" />
    <br />
    <button class="login-btn" onclick="login()">⚪</button>
    <div id="error" class="error"></div>
  </div>

  <script>
    function login() {
      const user = document.getElementById('username').value;
      const pass = document.getElementById('password').value;
      const errorDiv = document.getElementById('error');

      if (user === 'Ash' && pass === 'pikachu') {
        alert('Welkom terug, Trainer ' + user + '!');
        errorDiv.textContent = '';
      } else {
        errorDiv.textContent = 'Oeps! Verkeerde trainergegevens.';
      }
    }
  </script>
</body>
</html>
